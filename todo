-- Supervised learning models.

-- apex rainbow

-- clean up the all of the memories, such that they are using a base class 
-- for main storage
    -- the prioritized experience replay should only use the trees for the priority
    -- all the data should be stored in numpy array outside of there.

-- remove all points in the plotter?

-Add in ability to clear logs and save current graph.

-Clean up creating neural networks more, so error checking is done from an base class.

-- unit testing

-- TODO compress all different "DQN" upgrades into the DQN file with configs